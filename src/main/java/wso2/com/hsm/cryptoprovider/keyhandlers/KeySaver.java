package wso2.com.hsm.cryptoprovider.keyhandlers;

import iaik.pkcs.pkcs11.Session;
import iaik.pkcs.pkcs11.objects.Key;

public class KeySaver {
    public static boolean saveKey(Session session, Key key) {
        boolean saved = false;

        return saved;
    }
}
